services:
  - type: web
    name: neon-wire-arena
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_EMAIL
        value: jacobu483@gmail.com
      - key: ADMIN_PASSWORD
        sync: false
